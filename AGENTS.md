# Канон

Это читает каждый, кто пишет код в этом репозитории: человек, Claude, Codex — разницы нет.
Прочитай целиком до первой правки. Дальше — короткие правила, каждое с причиной: правило без
причины нарушают первым.

---

## 1. Дизайн-система — не рекомендация

Интерфейс собран на **shadcn/ui** (стиль `radix-nova`) и иконках **Lucide**. Это не «вдохновение»,
а источник компонентов.

**Нужен компонент — установи его:**

```bash
cd web && npx shadcn@latest add tabs
```

**Нельзя:** писать свой аналог существующего компонента, копировать из него куски, «временно»
собирать кнопку из `div` с классами. Если компонента нет в реестре — сначала посмотри, не
собирается ли нужное из уже установленных (`item`, `field`, `empty`, `bubble`).

Отступить можно ровно тогда, когда следование системе — блокер, и тогда рядом стоит комментарий
с причиной. Без комментария это просто самодеятельность, и её откатят.

**Почему:** каждая самодельная кнопка — это ещё один набор состояний, которые никто не поддержит:
фокус, disabled, тёмная тема, клавиатура, RTL. Система их уже держит.

**Состояния не изобретаем.** Активный пункт — `bg-accent text-accent-foreground`, фокус — кольцо
самого компонента, наведение — `hover:bg-accent/50`. Своя рамка, своё кольцо, своя подсветка
активного — это не полировка, а второй набор состояний рядом с системным.

**Состояние видно без легенды.** Текущий режим помечен бейджем «Текущий», а не инверсией
цвета, которую надо разгадывать, и не подписью под карточкой.

**Один шаблон, а не особый случай.** Встроенный режим настраивается той же карточкой, что
и остальные; человек в ленте нарисован тем же компонентом, что агент. Особый случай — это
ветка в коде, которую потом забудут обновить.

**Мы не улучшаем систему, а пользуемся ею.** Свой отступ, свой кегль, своя тень «чтобы было
красивее» — самый быстрый способ сломать экран на другой теме и другой ширине. Красиво здесь
значит «как в shadcn».

**`cn` в этом проекте не сливает классы.** Он из пакета `cn`, а не `tailwind-merge`:
`cn("h-9", "h-full")` оставит оба класса, и победит тот, что ниже в собранном CSS, — то есть
базовый класс компонента. Переопределять его строкой бесполезно: берём утилиту, которая
не конфликтует (`min-h-9` вместо `h-9`). По той же причине правило компонента, написанное
под `dark:`, переживёт твоё без `dark:`: перекрываешь такое — пиши обе версии.

## 2. Всё живёт на токенах

Цвет, размер, отступ, радиус, тень — только через токены темы (`web/src/index.css`) и шкалу
Tailwind.

- **Цвет:** `--background`, `--foreground`, `--muted-foreground`, `--border`, `--destructive`…
  Палитра участников — тоже токены: `--tone-<цвет>` (тон) и `--chroma-<цвет>` (насыщенность),
  а классы `.tone-face`, `.tone-name`, `.tone-bubble` считают из них конечный oklch.
  Заливка — прозрачность (`--tone-fill`), а не готовый цвет: так одна шкала работает и в тёмной
  теме, и в светлой. Текст прозрачностью не задать, его светлота переключается темой
  (`--tone-ink`, `--tone-label`, `--tone-accent`). Сами `.tone-*` объявлены вне `@layer`
  намеренно: иначе вариант компонента перебивает цвет участника.
- **Размер:** классы шкалы (`text-sm`, `size-8`, `gap-3`). Не `style={{ fontSize: 13 }}`.
- **Никаких hex в компонентах.** Появился новый цвет — он появляется в теме, а не по месту.

Единственное законное исключение — величина, которой в шкале нет (например `max-h-[85vh]`
у диалога). Тогда рядом комментарий, почему.

**Почему:** тема меняется в одном файле. Пиксель, вписанный в компонент, не меняется никогда.

## 3. DRY: одна причина — одно место

Если знание повторяется дважды, второе место — будущая рассинхронизация.

| Знание | Единственное место |
|---|---|
| Палитра и тема | `web/src/index.css` |
| Размеры аватарки | `FACE_SIZES` в `web/src/components/chat-feed.tsx` |
| Выбор цвета и знака | `web/src/components/face-picker.tsx` |
| Тексты оболочки | `web/src/lib/i18n.tsx` |
| Русская типографика | `web/src/lib/typo.ts` |
| Обращения к серверу | `web/src/lib/api.ts` |
| Состояние комнаты на диске | `lib/state.js` |
| Поведение роли | `roles/<роль>.md` |
| Голос участника | `archetypes/<амплуа>.md` |
| Устройство режима | `modes/<режим>.md` |
| Что участник умеет руками | `lib/skills.js` |
| Цена вопроса в ходах и токенах | `lib/rounds.js` |
| Значения по умолчанию | `lib/config.js` → `DEFAULTS` |

Роли, амплуа и режимы — файлы, а не код: они перечитываются на каждый ход, правка видна
без перезапуска. Новая роль, амплуа или режим = новый файл, а не ветка в `if`.

Роль отвечает на «что участник делает», амплуа — на «как он говорит». Держать их вместе
нельзя: один голос носят несколько ролей, и второй экземпляр того же текста в файле роли
не влияет ни на что, а правят его в первую очередь.

## 3.1. Иерархия инструкций

Правила приходят с трёх сторон, и порядок между ними объявлен в самом промпте — иначе они
спорят молча, и побеждает тот, кто сформулирован конкретнее:

1. **Пространство** — его законы и общие правила чата. Их не перебивает ничто.
2. **Режим** — пока идёт шаг, он сильнее обычной работы участника.
3. **Личное** — роль и голос. Меняют содержание и форму реплики, но не отменяют ничего выше.

Отсюда правило для файлов ролей: **предписанной формы фразы в роли быть не должно.**
«Решение принято — одной строкой: „Решено: <что>"» побеждало общее правило про живую речь,
и участник отвечал строкой статуса на «ага, ок, передал». Роль говорит, на что смотреть
и чего не уступать; как это звучит — дело голоса.

## 4. Понятия не смешиваются

Онтология продукта описана в [docs/concept.md](docs/concept.md) и в коде держится буквально:

```
Space     — где            Agent   — кто
Skill     — что умеет      Context — что знает
Mode      — как работаем   Rule    — что нельзя
Memory    — что усвоили
```

Знак над лентой держит ту же сетку и читается одним правилом: **слева — чем заняты,
в скобках — над чем**. Правая половина — предмет разговора (`article`, `memory`), а не
занятие: `open(validation)` говорит о комнате ровно ничего. Левая — действие: пока идёт
режим, это его слаг (`roast`, `review`) и ничто другое его не перебивает; вне режима
действие вычисляется из разговора тем же пробником, что и предмет (`validation(article)`).
Оба слова латиницей и строчными: это вызов функции, а не подпись.

Участник отвечает на вопрос «кто», режим — «как мы сейчас работаем». Три вопроса про людей
в комнате живут в трёх разных местах и не смешиваются:

- **Кто вообще есть** — каталог: Настройки → Участники. Там заводят и удаляют.
- **Кто сейчас в этой комнате** — тумблеры за кнопкой состава под полем ввода. Это свойство
  комнаты (`off` в её состоянии), а не участника: в одной комнате он есть, в другой нет.
- **Кто отвечает без тега** — дежурные режима, `modes/<режим>.md`.

Когда новая настройка не находит себе места в этой сетке — почти всегда неправильно назван
не файл, а сама настройка.

Человек — такой же участник: то же имя, та же аватарка из той же палитры, та же разметка
реплики. Отдельного «You» вместо имени и отдельной ветки в коде для него нет — как только
такая ветка заводится, за ней тянется второй набор правил, который никто не поддерживает.

## 5. Тексты интерфейса

Оболочка говорит на двух языках, и оба лежат одним словарём в `web/src/lib/i18n.tsx`: русская
и английская строки стоят рядом, поэтому не разъезжаются по смыслу. Литерала в JSX быть
не должно. Данные пространства — ники, роли, режимы — не переводятся: их завёл человек.

Тон — для взрослого человека.

- **Говорим, что произойдёт**, а не что система думает о себе: «Пауза. Начатые ответы
  допишутся», а не «Стоп».
- **Без канцелярита и без «Упс!».** Ошибка — это факт: «сервер вернул 500».
- **Всё длиннее трёх слов — через `typo()`**: предлоги и числа не должны висеть в конце строки.
- **Плейсхолдер — пример, а не инструкция:** «Собрать к пятнице спецификацию мегаменю»,
  а не «Введите цель».
- **Подпись не объясняет очевидное.** Полю «Имя» не нужна строка «как вас зовут». Описание
  уместно там, где без него выберут не то: уровень доступа, короткий режим.

## 6. Комментарии объясняют «почему»

Комментарий, пересказывающий код, — шум. Комментарий, объясняющий решение, — единственное
место, где это решение сохраняется.

```js
// Порядок участников сохраняем: он виден в шапке.
for (const [k, v] of Object.entries(s.agents)) agents[k === name ? next : k] = v
```

Язык комментариев — русский, как и весь разговор вокруг проекта. Идентификаторы — английские.

## 7. Проверка перед коммитом

```bash
npm test                     # движок на заглушках, токены не тратит
npm run dev                  # сервер и пересборка клиента на каждую правку
cd web && npm run build      # разовая сборка, тип-чек входит в неё
```

Участник правит файлы, но команд не запускает: при `npm run dev` его правка в интерфейсе
появляется сама, просить человека собрать не нужно.

Меняешь то, кого и когда зовут, — пишешь тест в `test/engine.test.mjs`. Оркестрацию глазами
не проверить: сбой там выглядит как участник, который «сам решил промолчать».

Меняешь сборку командной строки движка или разбор его ответа — тест в `test/cli.test.mjs`.
Эта граница ломается молча: чужой CLI меняет вывод, и комната просто замолкает.

На пуш в `main` то же самое гоняет CI (`.github/workflows/check.yml`).

Сломанная сборка в `main` дороже любой скорости. Правку интерфейса проверяем в браузере:
открыть экран, снять скриншот, сравнить с тем, что просили. Код, который компилируется,
не доказывает ничего — половина правок этого проекта выглядела в браузере иначе, чем в голове.

## 8. Git

- Ветка `main`, коммиты по-русски, повелительное наклонение: «убрать рамку у обращений».
- Одна мысль — один коммит.
- **Заголовок говорит, что изменилось**, а не что сделано в коде: «ответ — полоской внутри
  поля», а не «рефакторинг компонента Quote». Читать его будет тот, кто ищет, когда в продукте
  появилась та или иная вещь, а не тот, кто правил этот файл.
- **Тело отвечает «зачем».** Что было не так, что из-за этого видел человек, почему починили
  именно так и чем за это заплатили. Без тела остаётся одно «что» — а «что» и так видно в дифе.
- **Словами продукта, потом словами кода.** «Полоса прокрутки включалась из-за двух десятков
  пикселей отступа, хотя все карточки были видны» — понятно всем; `min-h-full` меряет высоту
  вместе с padding — строкой ниже, для того, кто полезет чинить.
- В описании пул-реквеста то же самое: что добавилось, когда и зачем, в том порядке, в каком
  это увидит человек, а не в порядке коммитов.
- Заметное для того, кто пользуется продуктом, попадает ещё и в [CHANGELOG.md](CHANGELOG.md).
  `git log` — история правок, CHANGELOG — история продукта: в нём нет ни рефакторингов,
  ни правок опечаток, зато есть причина, по которой вещь стала другой.
- `/public/` и `rooms/` не коммитим: это сборка и данные машины. Слэш в начале обязателен,
  иначе правило ловит и `web/public/`, а это исходники.
- `git add <файлы>`, а не `-A`: над репозиторием работают несколько сессий сразу, и `-A`
  уносит в коммит чужую правку на середине.

---

## Грабли

Каждая уже стоила сломанного экрана или потерянной реплики.

- **Лента открыта наружу.** Сервер слушает localhost, но браузер ходит на localhost с любой
  страницы, а реплика здесь — команда агенту с доступом к файлам. Поэтому у не-GET проверяется
  `Origin`, у всех запросов — `Host`, а полю `from` верят, только если такой участник есть
  в составе. Новый POST наследует эти проверки — они стоят до разбора маршрута.
- **Состав команды.** Настройки шлют ростер целиком и обязаны ставить `replaceTeam: true`,
  иначе сервер домешивает прежний состав и переименование оставляет дубль. По той же причине
  `loadConfig` не мешает `DEFAULTS.agents` с диском: убранный участник не должен воскресать.
- **Кириллица в регулярках.** Везде, где разбираются ники и имена файлов, диапазон `Ѐ-ӿ`
  обязателен. Без него имя комнаты превращается в подчёркивания, а обращение не распознаётся.
- **`total_cost_usd` из `claude -p` — не деньги,** а пересчёт по API-тарифу. Оба движка
  работают по подписке, поэтому в интерфейсе расход показан в токенах.
- **Флекс режет текст.** Половины знака лежат во флекс-строке; без `flex: none` они ужимаются
  под ширину якоря, и слова обрезаются.
- **Хук после раннего возврата роняет всё окно.** `if (!cfg) return <Скелет/>` и `React.useEffect`
  ниже — порядок хуков скачет между рендерами, и падает не компонент, а приложение: чёрный
  экран вместо настроек. Все хуки стоят до любого `return`, включая памятки и подписки.
- **Сервер держит состояние в памяти.** Правка в `lib/` или `server.js` видна только после
  перезапуска `npm run dev`; правка в `web/src/` — сразу.

## Карта репозитория

```
server.js               HTTP, SSE, REST — один файл, без фреймворка
lib/
  orchestrator.js       кого будить и когда; движок режимов
  agents.js             запуск claude -p и codex exec
  skills.js             что участник умеет руками и какие это флаги CLI
  modes.js              разбор modes/*.md, шаги из текста и обратно
  roles.js              разбор roles/*.md, оси и полюса
  archetypes.js         разбор archetypes/*.md — готовые голоса
  prompt.js             сборка системного промпта участника
  mentions.js           разбор @обращений и ответов на реплику
  memory.js             память пространства: решения, уроки, находки
  rounds.js             во что обошёлся вопрос — ходы, токены, минуты
  text.js               разворот жёстких переносов внутри абзаца
  store.js              лента комнаты, rooms/<комната>.jsonl
  state.js              режим, шаг, пауза, сессии — чтобы комната пережила перезапуск
  config.js             DEFAULTS и openspace.config.json
roles/                  по файлу на роль: что делает, зона интереса, полюс, домен
archetypes/             по файлу на амплуа: как говорит
modes/                  по файлу на режим, отложенные — в modes/архив/
web/src/                клиент: React + Vite + Tailwind 4 + shadcn/ui
web/public/             фавиконка и знаки — исходники, не сборка
test/                   движок и разбор файлов на заглушках, `npm test`
bin/                    dev, say, hire, duel — из терминала
docs/                   концепция, устройство, научная база групповой динамики
CHANGELOG.md            что менялось в продукте и почему
```

## Как добавить

**Роль** — файл `roles/<имя>.md`, формат в [roles/README.md](roles/README.md).
**Режим** — файл `modes/<имя>.md`, формат в [modes/README.md](modes/README.md), либо прямо
в интерфейсе: Настройки → Режимы. Редактор пишет тот же файл.
**Компонент** — `npx shadcn@latest add <имя>` в `web/`, потом использовать как есть.


<claude-mem-context>
# Memory Context

# [openspace] recent context, 2026-09-20 12:03am GMT+3

Legend: 🎯session 🔴bugfix 🟣feature 🔄refactor ✅change 🔵discovery ⚖️decision
Format: ID TIME TYPE TITLE
Fetch details: get_observations([IDs]) | Search: mem-search skill

Stats: 50 obs (14,515t read) | 241,536t work | 94% savings

### Sep 19, 2026
S652 Prostranstvo — docs/plan.md cross-referenced with Opus 5 todo.md using T1–T18 tags (Sep 19 at 12:45 PM)
S651 openspace review + active refactoring session — comprehensive code/UX/architecture review followed by applying fixes live in the codebase (Sep 19 at 12:45 PM)
S653 Prostranstvo — "делай что не доделал": завершение этапа 2, движок режимов, правка реплик, UI-полировка (Sep 19 at 12:54 PM)
S654 openspace — режимы: критерии отбора, восстановление «Шести шляп» и «Премортема», исследование Методотеки (Sep 19 at 1:23 PM)
S655 Openspace READMEs updated to reference docs/dynamics.md literature review (Sep 19 at 1:30 PM)
S656 Prostranstvo — коллективные числительные для счётчика участников (Sep 19 at 1:51 PM)
S657 Prostranstvo settings General tab — visual layout verified via browser screenshot (Sep 19 at 2:03 PM)
S659 Openspace settings dialog — убрать лишние подписи под полями и упростить подпись автора (Sep 19 at 3:11 PM)
2979 3:40p 🔴 Prostranstvo тест 21 исправлен — все 25 тестов зелёные
2980 3:41p 🔄 Prostranstvo face-picker.tsx — иконки аватарки сокращены с 32 до 24 и сгруппированы по смыслу
2981 3:46p 🔴 Settings Icon Incorrect — Should Be Gear/Cog
2982 3:47p 🔴 Settings Icon Fixed: Settings2 → Settings in Lucide React
2983 " 🔴 Settings Icon TS Conflict Resolved via Import Alias
2984 " ✅ Settings Icon Fix Verified — All 25 Tests Pass
2985 3:48p 🔵 Цвет UI-элемента реализован через цвет, а не прозрачность
2986 " 🔵 UI цвет реализован статическим цветом, а не прозрачностью
2987 3:49p 🔵 Openspace — цвета пузырей реализованы как непрозрачные oklch(), а не через прозрачность
2988 3:50p 🔴 Openspace — цвета пузырей переведены с непрозрачных oklch() на прозрачную заливку с темовыми переменными
2989 " 🟣 Openspace settings-dialog — рефакторинг геометрии и структуры General-вкладки
2990 3:51p 🔄 Openspace agent-card — строка списка переработана: убрана рамка, brain вынесен вправо, min-height 64px
2991 " 🔄 Openspace — списки агентов и режимов переведены с gap-2 карточек на divide-y строки
2992 " 🔴 Openspace — удалён лишний импорт `cn`, геометрия диалога подтверждена браузером
2993 3:52p 🔴 Openspace NAV — `h-9` заменён на `min-h-9` для пунктов навигации в settings-dialog
2994 3:58p ⚖️ Ясно UI — аватар должен загружаться до основного контента
2995 " 🔴 Prostranstvo — аватарка перемещена выше поля «Имя» в настройках
2996 3:59p 🔵 Prostranstvo settings dialog — QA метрики всех четырёх вкладок подтверждены
2997 " 🔴 Prostranstvo — вкладка «Пространство» получила max-w-xl ограничение ширины контента
2998 4:00p 🔵 Prostranstvo — focus-visible на табах работает при навигации с клавиатуры
2999 4:01p 🔴 Prostranstvo — восстановлена видимость фокуса с клавиатуры на табах диалога настроек
3000 " 🔵 Prostranstvo settings dialog — финальный QA пройден: 25/25 тестов, все вкладки 576px
3001 4:02p 🔵 Prostranstvo — диагностика: CSS-переменная --ring установлена, ring-ring на обычных элементах работает
3002 4:03p 🟣 Prostranstvo settings dialog — комплексный UX-полиш: аватарка, табы, кнопка режима, i18n
3003 4:04p 🔄 Prostranstvo — форма приглашения участника переведена на прогрессивное раскрытие
3004 " 🔵 Prostranstvo — базовый компонент таба содержит собственные focus-visible:ring классы
3005 " 🔵 Prostranstvo settings dialog — финальные метрики боковой панели навигации
3006 4:07p 🔵 Openspace участники — измерение отступов и цветов модели
3007 4:08p 🔴 Openspace — исправлены отступы, цвет и ширина колонки модели в карточках участников
3008 4:09p 🔴 Openspace — откат -mr-2 с кнопки меню и переименование поля space.goal
3009 4:10p ✅ Openspace i18n — переработаны строки формы создания участника
S660 Openspace — исправить отступы справа, позиционирование и цвет названия модели в панели участников (Sep 19 at 4:10 PM)
3010 4:11p 🔵 Prostranstvo — settings-dialog.tsx содержит логику найма агентов
3011 4:12p 🟣 Prostranstvo — новый агент автоматически открывает свою карточку после создания
3012 " 🟣 Prostranstvo — autoOpen агента подтверждён QA-тестом в браузере
3013 " 🔵 Prostranstvo — API /api/settings поддерживает удаление агентов через replaceTeam
3014 4:20p 🔵 Openspace — favicon и структура public/ изучены перед созданием новой иконки
3015 4:21p 🔵 Openspace — директория public/ полностью gitignored как артефакт сборки
3016 4:22p 🟣 Openspace — создана SVG-фавиконка o(s) с поддержкой тёмной темы
3017 " 🔴 Openspace .gitignore — исправлен паттерн public/ на /public/ чтобы не игнорировать web/public
3018 " 🔵 Openspace favicon.svg — верификация рендеринга: 200 OK, текст 29×15px, смещение x=2
3019 4:23p 🔵 Openspace favicon — изменения не закоммичены, web/public/ untracked
3020 " 🔵 Openspace orchestrator — архитектура state() и duty() методов
3021 " 🔵 Openspace orchestrator — save() дебаунсится 200ms, flush() пишет seen+sessions всех агентов
3022 " 🟣 Openspace orchestrator — per-room выключение участников: поле off[], методы here() и toggle()
3023 4:24p 🟣 Openspace orchestrator — полная замена this.names → this.here(room) во всех точках таргетинга
3024 " 🟣 Openspace — новый HTTP endpoint POST /api/presence для управления присутствием агента в комнате
3025 4:25p 🔵 Openspace — тест per-room presence упал: выключенный агент всё ещё отвечает на тег или дежурным
3026 " 🔵 Openspace — тест №22 упал из-за BUILTIN-режима: свободный.md явно именует @второй в duty
3027 4:26p 🔴 Openspace — тест toggle исправлен: проверка через .some() вместо точного счётчика, 26/0
3028 " 🔵 Openspace фронтенд — UI участников и api.ts перед добавлением presence

Access 242k tokens of past work via get_observations([IDs]) or mem-search skill.
</claude-mem-context>