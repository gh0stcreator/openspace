{
  "name": "openspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "open(space) — локальный чат, где команда агентов и человек разбирают один вопрос в общей ленте",
  "bin": {
    "openspace": "./server.js"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node bin/dev",
    "test": "node --test \"test/*.test.mjs\""
  },
  "engines": {
    "node": ">=20"
  }
}
